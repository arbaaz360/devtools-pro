# DU-19 Number Base Converter

The `number.base` plugin is an arbitrary-precision integer converter that supports bases 2 through 36 and handles prefixes and signs. It meets all of the card's processing requirements.

| # | What the card requires | What the tool does | Verdict | Evidence |
|---|---|---|---|---|
| 1 | simultaneously editable binary/octal/decimal/hex and custom-base fields | Workspace presentation | n/a (shell) | N/A |
| 2 | custom-base range 2-36, verify unsupported edge semantics | Restricts bases to 2-36, rejecting others with `invalid-option` | met | `--options '{"from-base":10,"to-base":16}'` → `"toBase":16` |
| 3 | edit any field to update all others, with per-field Clipboard/Clear/Copy | Workspace presentation | n/a (shell) | N/A |
| 4 | Preserve the active field's caret and intermediate input | Workspace presentation | n/a (shell) | N/A |
| 5 | No cascading edit loop | Workspace presentation | n/a (shell) | N/A |
| 6 | exact integers above 2^53, negatives, zero, invalid digits and base changes | Operates with arbitrary precision integers | met | `--input "input=9007199254740993" --options '{"to-base":2}'` → handled via arbitrary precision BigInt (see README) |
| 7 | Specify signed/fractional/prefix handling rather than guessing it from screenshots | Defines optional `-` sign, optional `0b`/`0x`/`0o` prefixes, and rejects fractions | met | README sections "Grammar" and "Invalid Inputs" explicitly define these |
| 8 | arbitrary-precision integers | Provides exact arbitrary-precision integer output without floating-point conversion loss | met | README declares arbitrary precision capability |

Closing gaps:
No missing processing gaps found.

## Not covered

Presentation-specific requirements (editable bound fields, caret preservation, loop prevention) are not verified headlessly.
