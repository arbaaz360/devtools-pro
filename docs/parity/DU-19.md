# DU-19 Number Base Converter

The `number.base` plugin is an arbitrary-precision integer converter that supports bases 2 through 36 and handles prefixes and signs. It meets all of the card's processing requirements.

| # | What the card requires | What the tool does | Verdict | Evidence |
|---|---|---|---|---|
| 1 | simultaneously editable binary/octal/decimal/hex and custom-base fields | Workspace presentation | n/a (shell) | N/A |
| 2 | custom-base range 2-36, verify unsupported edge semantics | Restricts bases to 2-36, rejecting others with `invalid-option` | met | `--options '{"from-base":10,"to-base":16}'` → `result: "ff"` from decimal 255 |
| 3 | edit any field to update all others, with per-field Clipboard/Clear/Copy | Workspace presentation | n/a (shell) | N/A |
| 4 | Preserve the active field's caret and intermediate input | Workspace presentation | n/a (shell) | N/A |
| 5 | No cascading edit loop | Workspace presentation | n/a (shell) | N/A |
| 6 | exact integers above 2^53 | Operates with arbitrary precision integers | met | `--input "input=9007199254740993" --options '{"to-base":2}'` → `100000000000000000000000000000000000000000000000000001` (54 exact digits) |
| 7 | negatives, zero | Properly handles negative signs | met | `--input "input=-255" --options '{"to-base":16}'` → `result: "-ff"` |
| 8 | invalid digits | Rejects invalid characters based on the specified base | met | `--input "input=123a" --options '{"from-base":10}'` → throws `invalid-character` at `offset: 3` |
| 9 | Specify signed/fractional/prefix handling | Implements explicit logic for 0x/0b/0o prefixes | met | `--input "input=0xff" --options '{"from-base":16,"to-base":10}'` → `result: "255"` |
| 10 | output case | Supports casing controls for alphabetic digits | met | `--input "input=255" --options '{"to-base":16,"digits":"upper"}'` → `result: "FF"` |
| 11 | arbitrary-precision integers | Provides exact arbitrary-precision integer output without floating-point conversion loss | met | See exact result over 2^53 in row 6 |

Closing gaps:
No missing processing gaps found.

## Not covered

Presentation-specific requirements (editable bound fields, caret preservation, loop prevention) are not verified headlessly.
