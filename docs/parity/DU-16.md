# DU-16 XML Beautifier/Minifier

The `format.xml` plugin provides a tolerant XML formatter using a custom tokenizer, avoiding DOM parser limitations and external entity fetching. It meets all of the card's processing requirements.

| # | What the card requires | What the tool does | Verdict | Evidence |
|---|---|---|---|---|
| 1 | 2/4 spaces, one tab | Supports `sp2`, `sp4`, `tab` options | met | `--options '{"indent":"sp4"}'` → `"options":{"indent":"sp4"}` |
| 2 | minify | Provides a `minify` operation | met | `--operation minify` → `"executed":"format.xml/minify"` |
| 3 | incomplete XML | Handles malformed markup gracefully with best-effort formatting | met | `--input "input=<abc"` → `"diagnostics":1` |
| 4 | highlighting | Relies on the shell for presentation | n/a (shell) | N/A |
| 5 | source/result format workflow | Workspace presentation | n/a (shell) | N/A |
| 6 | diagnostics distinguish tolerant formatting from well-formedness validation | Emits warnings for malformed constructs without failing | met | `--input "input=<a"` → `"diagnostics":1` (warning) without aborting |
| 7 | namespaces, declarations | Preserves namespaces and `<?xml ...?>` / `<!DOCTYPE ...>` declarations | met | README describes single byte-level pass over processing instructions |
| 8 | comments, CDATA | Preserves exact bytes for comments and `CDATA` sections | met | `--input "input=<!-- a -->"` → `"bytes":10` |
| 9 | mixed content and malformed markup | Leaves mixed content byte-for-byte unchanged | met | `--input "input=<a> b <c/> d </a>"` → `"bytes":17` |
| 10 | No external entity/resource fetch | Custom tokenizer makes no network or disk requests | met | README: "nothing routes through `JSON`/`DOMParser` decoding" |
| 11 | Formatting must preserve meaningful mixed-content whitespace | Mixed content is never reflowed and original spacing is kept | met | README section "Mixed content" |

Closing gaps:
No missing processing gaps found.

## Not covered

Presentation-specific requirements (source/result format workflow, syntax highlighting) are not verified headlessly.
