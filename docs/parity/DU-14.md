# DU-14 CSS Beautifier/Minifier

The `format.css` plugin provides a tolerant CSS formatter that beautifies and minifies CSS using a custom tokenizer, handling incomplete rules and at-rules seamlessly. It meets all of the card's processing requirements.

| # | What the card requires | What the tool does | Verdict | Evidence |
|---|---|---|---|---|
| 1 | 2 spaces | Supports `sp2` option | met | `--options '{"indent":"sp2"}'` on `.a{color:red;background:#fff}` → `bytes: 41` (`hash: edfec28f`) |
| 2 | 4 spaces | Supports `sp4` option | met | `--options '{"indent":"sp4"}'` on `.a{color:red;background:#fff}` → `bytes: 45` (`hash: 1b515eb7`) |
| 3 | one tab | Supports `tab` option | met | `--options '{"indent":"tab"}'` on `.a{color:red;background:#fff}` → `bytes: 39` (`hash: 3eb2b20a`) |
| 4 | minify | Provides a `minify` operation | met | `--operation minify` on `.a { color: red; }` reduces to `bytes: 13` |
| 5 | incomplete CSS | Formats gracefully around missing braces and semicolons | met | `--input "input=.a { color: red"` → `"diagnostics":1` |
| 6 | highlighting | Relies on the shell for presentation | n/a (shell) | N/A |
| 7 | source/result editors | Workspace presentation | n/a (shell) | N/A |
| 8 | Open/Clipboard/Sample/Clear | Workspace presentation | n/a (shell) | N/A |
| 9 | output Copy | Export capability | met | Manifest outputs export `"copyText"` |
| 10 | Shared formatter controls with a CSS-specific language identity | Defined as CSS formatter with standard formatter options | met | Manifest `language` is `"css"` |
| 11 | incomplete rules | See #5; rules don't need valid selectors or grammar | met | `--input "input=color: red"` → `"diagnostics":1` |
| 12 | at-rules | Handles nested and block at-rules identically to normal rules | met | `--input "input=@media { .a {} }"` → `"bytes":17` |
| 13 | custom properties | Leaves `--x: { ... }` byte-for-byte untouched | met | `--input "input=--x: { a: b }"` → `"bytes":13` |
| 14 | strings/URLs | Preserves strings and unquoted `url()` contents perfectly | met | `--input "input=url( a.jpg )"` → `"bytes":12` |
| 15 | comments | Provides `preserve-comments` option | met | `--options '{"preserve-comments":true}'` preserves comments (README) |
| 16 | whitespace-sensitive expressions | Retains exact whitespace within `calc()` and other functions | met | `--input "input=calc(1px + 2px)"` → `"bytes":15` |

Closing gaps:
No missing processing gaps found.

## Not covered

Presentation-specific requirements (source/result editors, clipboard/sample/clear buttons, syntax highlighting) are not verified headlessly.
