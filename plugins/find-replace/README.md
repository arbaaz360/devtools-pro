# Find & Replace

The v2 `text.find-replace` package searches literal Unicode text, reports UTF-16 match offsets with a bounded 10,000-match result, and performs literal first/all replacements. It never evaluates a regular expression or invokes a shell, preserves the input document, and enforces UTF-8, size, and cancellation limits.
